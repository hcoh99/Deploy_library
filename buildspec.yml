version: 0.2

phases:
  install:
    commands:
      - echo Installing kubectl...
      - curl -O https://s3.us-west-2.amazonaws.com/amazon-eks/1.31.0/2024-09-12/bin/linux/amd64/kubectl
      - chmod +x ./kubectl
      - mkdir -p $HOME/bin && cp ./kubectl $HOME/bin/kubectl && export PATH=$PATH:$HOME/bin

  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region us-east-2 | docker login --username AWS --password-stdin 416225317325.dkr.ecr.us-east-2.amazonaws.com
      - REPOSITORY_URI=416225317325.dkr.ecr.us-east-2.amazonaws.com/hcoh-library-proj
      - COMMIT_HASH=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
      - IMAGE_TAG=${COMMIT_HASH:=latest}

  build:
    commands:
      - echo Build started on `date`
      
      # [디버깅용] 현재 파일들이 어디에 있는지 전체 목록을 출력합니다.
      # (만약 에러가 나면 이 로그를 보고 파일 위치를 찾을 수 있습니다!)
      - echo "=== File List Debugging ==="
      - ls -R
      - echo "==========================="

      - echo Building the Docker image...
      # 폴더 안으로 들어가지 않고, 밖에서 경로(librarysystem_front)를 지정해서 빌드합니다.
      - docker build -t $REPOSITORY_URI:latest librarysystem_front
      - docker tag $REPOSITORY_URI:latest $REPOSITORY_URI:$IMAGE_TAG
      
      - echo Pushing the Docker image...
      - docker push $REPOSITORY_URI:latest
      - docker push $REPOSITORY_URI:$IMAGE_TAG
      
      - echo Updating kubeconfig...
      - aws eks update-kubeconfig --name hcoh-library-cluster --region us-east-2
      
      - echo Deploying to EKS...
      # kubectl 명령어도 폴더명을 직접 적어서 실행합니다.
      - kubectl apply -f librarysystem_front/deployment.yaml
      - kubectl apply -f librarysystem_front/service.yaml
      
      - echo Deployment completed!

  post_build:
    commands:
      - echo All done!